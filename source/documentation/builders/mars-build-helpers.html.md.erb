---
title: mars-build-helpers
layout: documentation
active: Builders, mars-build-helpers
example: builders/helpers
---

# mars-build-helpers

marscss e allows to include different classes of css to function as helpers.

Helpers are usually small variations in appearance that force the over-writing of some property by `!important`

When calling mars-buil-helpers all the different helpers are included, it is preferable to choose which helpers you will need to not generate unnecessary css classes in your document.

since
: 1.0.0

core
: helpers

arguments
: `$prefixes : ''` *[string]*:
: `$colors : $colors` *[map]*:

size compiled
: <%= file_dist_size %> not minified

<%= source %>

<% example_tabs do %>
<%= file_example_scss %>
<%= file_example_dist %>
<% end %>


## mars-build-helpers-align

It is a set of helpers to work with the alignment

since
: 1.0.0

core
: helpers

arguments
: `$prefix: 'align-'` *[string]*:

size compiled
: <%= file_dist_size('buildersHelpersAlign') %> not minified

<% example_tabs do %>
<%= file_example_scss('builders/helpers-align') %>
<%= file_example_dist('buildersHelpersAlign') %>
<% end %>


## mars-build-helpers-border

This set of helpers allows you to have control over the edge of an element, inheriting the color of the element's text as the border color.

since
: 1.0.0

core
: helpers

arguments
: `$prefix: 'border'` *[string]*

size compiled
: <%= file_dist_size('buildersHelpersBorder') %> not minified

<% example_tabs do %>
<%= file_example_scss('builders/helpers-border') %>
<%= file_example_dist('buildersHelpersBorder') %>
<% end %>


## mars-build-helpers-colors

Use the color array to generate a class for each of the colors, which modifies the color of the element

since
: 1.0.1

core
: helpers

arguments
: `$prefix: 'color-'` *[string]*:
: `$colors: $colors` *[map]*:

size compiled
: <%= file_dist_size('buildersHelpersColor') %> not minified

<% example_tabs do %>
<%= file_example_scss('builders/helpers-color') %>
<%= file_example_dist('buildersHelpersColor') %>
<% end %>


## mars-build-helpers-fs

Assign the font size from the values of the different elements of the application

since
: 1.0.0

core
: helpers

arguments
: `$prefix: 'fs-'` *[string]*

size compiled
: <%= file_dist_size('buildersHelpersFs') %> not minified

<% example_tabs do %>
<%= file_example_scss('builders/helpers-fs') %>
<%= file_example_dist('buildersHelpersFs') %>
<% end %>


# mars-build-helpers-pull

Add float right or left to the element

since
: 1.0.0

core
: helpers

arguments
: `$prefix: 'pull-'` *[string]*

size compiled
: <%= file_dist_size('buildersHelpersPull') %> not minified

<% example_tabs do %>
<%= file_example_scss('builders/helpers-pull') %>
<%= file_example_dist('buildersHelpersPull') %>
<% end %>


## mars-build-helpers-ratio

Adjusts the height of an element by a proportion of its width, it is very useful for iframes or elements with background images.

It requires of 2 elements the parent element that contains the class and the child element that is resized to the size of the parent.

since
: 1.0.1

core
: helpers

arguments
: `$prefix: 'ratio-'` *[string]*

size compiled
: <%= file_dist_size('buildersHelpersRatio') %> not minified

<% example_tabs do %>
<%= file_example_scss('builders/helpers-ratio') %>
<%= file_example_dist('buildersHelpersRatio') %>
<% end %>


## mars-build-helpers-text

A set of rules to work the presentation of the text, includes alignments, nowrapp, truncate, etc ...

since
: 1.0.0

core
: helpers

arguments
: `$prefix: 'text-'` *[string]*

size compiled
: <%= file_dist_size('buildersHelpersText') %> not minified

<% example_tabs do %>
<%= file_example_scss('builders/helpers-text') %>
<%= file_example_dist('buildersHelpersText') %>
<% end %>


## mars-build-helpers-utilities

A set of utilities that can be useful in the application or when we work the presentation of elements using javascript

since
: 1.0.0

core
: helpers

arguments
: `$prefix: ''` *[string]*

size compiled
: <%= file_dist_size('buildersHelpersUtilities') %> not minified

<% example_tabs do %>
<%= file_example_scss('builders/helpers-utilities') %>
<%= file_example_dist('buildersHelpersUtilities') %>
<% end %>
