---
title: buttons components
layout: documentation
active: Components, buttons
---

#Buttons

Component to make all kinds of buttons, you can make as many beautiful and decorative buttons as you want.

since
: 1.0.0

If you only need a simple button you can call the mixin directly as the following example.

<div class="alert">
If you need to generate several buttons it is preferable to use @extend from the common block of content, in this way we will generate only the css that is different in both buttons.
</div>

<% example_tabs do %>
~~~scss
.orange-button {
  @include mars-button;
  background-color: $orange;
  color: $white;
  &:hover,
  &:focus,
  &:active,
  &.is-active {
    background-color: darken($orange,10%);
  }
}
~~~

~~~css
.orange-button {
  /* ///////// 🛸 mars-button */
  padding: 0.4em 1em;
  cursor: pointer;
  border: 1px solid;
  border-radius: 5px;
  transition: color .2s ease-out, background-color .2s ease-out;
  background-color: #f9b13d;
  color: #fff; }
  .button:hover, .button:focus, .button:active, .button.is-active {
    background-color: #f79d0c; }
~~~

<% example_html do %>
<button class="orange-button">button</button>
<% end %>
<% end %>


##Create multiple buttons

To generate multiple alerts you can use the generic @mixin <%= link_to 'mars-build-components','documentation/builders/mars-build-components' %>, visit the page on the builder to better understand how it works and arguments.

<% example_tabs do %>
~~~scss
%button {
  @include log('📦 extend %button');
  @include mars-button;
}

@include mars-build-components(
  $prefix: '.btn',
  $extend: '%button',
  $elements: $mars-buttons
);
~~~

~~~css
.btn-primary, .btn-default {
  /* ///////// 📦 extend %button */
  /* ///////// 🛸 mars-button */
  padding: 0.4em 1em;
  cursor: pointer;
  border: 1px solid;
  border-radius: 5px;
  transition: color .2s ease-out, background-color .2s ease-out; }

/* ///////// ☄️ mars-build-components .btn */
.btn-primary {
  color: #fff;
  background-color: #dc3545; }
  .btn-primary:active, .btn-primary:focus, .btn-primary:hover, .btn-primary.is-active {
    background-color: #bd2130; }

.btn-default {
  color: #fff;
  background-color: #495057; }
  .btn-default:active, .btn-default:focus, .btn-default:hover, .btn-default.is-active {
    background-color: #32373b; }
~~~

<% example_html do %>
<button class="btn-primary">btn-primary</button>
<button class="btn-default">btn-default</button>
<% end %>
<% end %>

The `$mars-buttons` contains the variations written on a map and its name is generated by adding the prefix that we pass to the mixin.



~~~scss
//we store the different selectors in a variable
$mars-button-active: '&:active, &:focus, &:hover, &.is-active' !default;

$mars-buttons:(
  '-primary': (
    color: $white,
    background-color: $primary,
    $mars-button-active: (
      background-color: darken($primary, 10%)
    )
  ),
  '-default':(
    color: $white,
    background-color: $gray-700,
    $mars-button-active: (
      background-color: darken($gray-700, 10%)
    )
  )
) !default;
~~~

##create multiple variations

If we would like, for example, to have other buttons with another size using the same elements, we could create a new `@extend`, in this example we will add all at once to be able to see the resulting css

<% example_tabs do %>

~~~scss
%button {
  @include log('📦 extend %button');
  @include mars-button;
}

@include mars-build-components(
  $prefix: '.btn',
  $extend: '%button',
  $elements: $mars-buttons
);

%button-sm {
  @include log('📦 extend %button-sm');
  @extend %button;
  font-size: $font-size-small;
}

@include mars-build-components(
  $prefix: '.btn-sm',
  $extend: '%button-sm',
  $elements: $mars-buttons
);
~~~

~~~css
.btn-primary, .btn-default, .btn-sm-primary, .btn-sm-default {
  /* ///////// 📦 extend %button */
  /* ///////// 🛸 mars-button */
  padding: 0.4em 1em;
  cursor: pointer;
  border: 1px solid;
  border-radius: 5px;
  transition: color .2s ease-out, background-color .2s ease-out; }

/* ///////// ☄️ mars-build-components .btn */
.btn-primary {
  color: #fff;
  background-color: #dc3545; }
  .btn-primary:active, .btn-primary:focus, .btn-primary:hover, .btn-primary.is-active {
    background-color: #bd2130; }

.btn-default {
  color: #fff;
  background-color: #495057; }
  .btn-default:active, .btn-default:focus, .btn-default:hover, .btn-default.is-active {
    background-color: #32373b; }

.btn-sm-primary, .btn-sm-default {
  /* ///////// 📦 extend %button-sm */
  font-size: 1.45455rem; }

/* ///////// ☄️ mars-build-components .btn-sm */
.btn-sm-primary {
  color: #fff;
  background-color: #dc3545; }
  .btn-sm-primary:active, .btn-sm-primary:focus, .btn-sm-primary:hover, .btn-sm-primary.is-active {
    background-color: #bd2130; }

.btn-sm-default {
  color: #fff;
  background-color: #495057; }
  .btn-sm-default:active, .btn-sm-default:focus, .btn-sm-default:hover, .btn-sm-default.is-active {
    background-color: #32373b; }
~~~

<% example_html do %>
<button class="btn-primary">btn-sm-primary</button>
<button class="btn-default">btn-sm-default</button>
<button class="btn-sm-primary">btn-sm-primary</button>
<button class="btn-sm-default">btn-sm-default</button>
<% end %>
<% end %>
