---
title: marscss components
layout: documentation
active: Components, Alerts
---

# Alerts


If you want to use alerts first you need to add the mars-alert style and classe to a css element. To do this you should add the mars-alert builder like:

<% example_tabs do %>

<% example_html do %>
<div class="alert">
  JPEG, PNG, GIF or BMP - 5MB file limit, at least 250x250 pixels.
</div>
<% end %>

~~~scss
.alert { @include mars-alert; }
~~~

~~~css
.alert {
  /* ///////// 🛸 mars-alert */
  padding: 1.5rem;
  color: #f9b13d;
  background-color: rgba(249, 177, 61, 0.04);
  border: 1px solid;
  border-radius: 3px;
  margin-top: 0;
  margin-bottom: 2rem; }
  .alert.is-dismissible {
    position: relative;
    padding-right: 3rem; }
  .alert a {
    color: inherit;
    border-bottom: 1px solid; }
    .alert a:hover {
      color: inherit; }
~~~
<% end %>


You can make use of the `mars-btn-close` component by adding the class `is-dismissible`.

<% example_tabs do %>
<% example_html do %>
<div class="alert is-dismissible">
  <h4>This is an alert sample!</h4>
  You can add <a href="#">links</a> or texts with <strong>format</strong>
  <button class="btn-close"></button>
</div>
<% end %>
<% end %>


##create multiple alerts

To generate multiple alerts you can use the generic @mixin <%= link_to 'mars-build-component', '/documentation/builders/mars-build-component' %> components, visit the page on the builder to better understand how it works and arguments.

The `$mars-alerts` element contains the variations written on a map and its name is generated by adding the prefix that we pass to the mixin.

~~~scss
$mars-alerts: (
  '': (),
  '-warning': (
    background-color: $warning,
    border-color: $warning,
    color: $white
  ),
  '-success': (
    background-color: $success,
    border-color: $success,
    color: $white
  )
);
~~~

In this case we add the first empty element to directly use the prefix as an element, so using this builder it is preferable that we do not add the mars-alert to another element that is not the element to be extended in this case% alert, thus we will obtain less duplicities of css in the result.

<% example_tabs do %>
~~~scss
%alert {
  @include log('📦 extend %alert');
  @include mars-alert();
}

@include mars-build-component(
  $prefix: '.alert',
  $extend: '%alert',
  $elements: $mars-alerts
);
~~~

~~~css
.alert, .alert-warning, .alert-success {
  /* ///////// 📦 extend %alert */
  /* ///////// 🛸 mars-alert */
  padding: 1.5rem;
  color: #f9b13d;
  background-color: rgba(249, 177, 61, 0.04);
  border: 1px solid;
  border-radius: 3px;
  margin-top: 0;
  margin-bottom: 2rem; }
  .is-dismissible.alert, .is-dismissible.alert-warning, .is-dismissible.alert-success {
    position: relative;
    padding-right: 3rem; }
  .alert a, .alert-warning a, .alert-success a {
    color: inherit;
    border-bottom: 1px solid; }
    .alert a:hover, .alert-warning a:hover, .alert-success a:hover {
      color: inherit; }

/* ///////// ☄️ mars-build-component .alert */
.alert-warning {
  background-color: #f9b13d;
  border-color: #f9b13d;
  color: #fff; }

.alert-success {
  background-color: #28a745;
  border-color: #28a745;
  color: #fff; }
~~~

<% example_html do %>
<div class="alert">
  JPEG, PNG, GIF or BMP - 5MB file limit, at least 250x250 pixels.
</div>

<div class="alert-success">
  Something really bad has happened.
</div>

<div class="alert-warning">
  This is fine and requires notification
</div>
<% end %>
<% end %>

